# Database
DB_PATH=./db.sqlite

# Email (Production)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=secret

# IMAP (for sent folder)
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_USER=user@example.com
IMAP_PASS=secret

# Development
NODE_ENV=development
PORT=5173
